kind = "mobile"
previewPath = "/"
title = "Мой Доход — для самозанятых"
version = "1.0.0"
id = "artifacts/selfemployed-tracker"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
ensurePreviewReachable = "/status"
name = "expo"
paths = [ "/" ]
localPort = 25579

[services.development]
run = "pnpm --filter @workspace/selfemployed-tracker run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/selfemployed-tracker", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/selfemployed-tracker", "run", "serve" ]

[services.env]
PORT = "25579"
BASE_PATH = "/"
